import { UniqueEmailValidation } from './unique-email-validation.directive';

describe('UniqueEmailValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueEmailValidation();
    expect(directive).toBeTruthy();
  });
});
